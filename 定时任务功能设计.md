# XWork 定时任务功能设计

> 状态：方案讨论稿（待评审修改）
> 日期：2026-08-10
> 参与：XWR & Agent

---

## 1. 背景与目标

为 XWork 增加「定时任务」能力：**在指定的工作区，按设定的时间计划，自动执行固定的 Agent 任务**，并在完成后通过系统通知反馈结果。

典型使用场景：

- 每天早上 9 点自动读取 CHANGELOG 生成昨日变更摘要并写入 daily.md
- 每周一运行一次代码质量巡检（lint / 测试）
- 每小时同步一次远程仓库状态到本地文件
- 应用启动时自动执行一次数据备份任务

## 2. 核心架构约束（必须了解的前提）

当前架构对定时任务最关键的约束来自 opencode 引擎的进程模型：

| 约束 | 说明 | 影响 |
| --- | --- | --- |
| 单进程单工作区 | 引擎以工作区目录为启动目录，`cwd` 是进程级属性（`engine.js`），会话按项目隔离 | 切换工作区必须重启引擎（数秒），且会中断用户当前会话上下文 |
| 权限引擎 | 权限规则来自全局 `opencode.json` 的 `permission` 段 | 无人值守任务必须配置「允许」档，否则会卡在权限确认弹窗 |
| 会话存储 | 会话数据存放在引擎数据目录（`XDG_DATA_HOME`，见 `xdgEnv`） | 不同数据目录的会话互相隔离，任务会话不会出现在主 UI 会话列表 |

**结论**：定时任务要在「指定工作区」执行，而用户可能正在「另一个工作区」工作。任务执行不能占用主引擎，否则会把用户「挤走」。

## 3. 关键决策（已确认）

| 决策点 | 结论 |
| --- | --- |
| 执行架构 | **独立引擎进程**：任务在第二个引擎实例（独立端口 + 独立数据目录 + 目标工作区）中执行，主引擎不受影响 |
| 调度形式 | **简化模板 + 高级 cron**：UI 提供简化模板（每天/每周几点、每 N 小时、应用启动时），高级模式可填 cron 表达式 |
| 运行前提 | **仅应用运行时**：应用退出（非托盘）后任务不执行；错过时间点按策略补跑/跳过 |
| 无人值守权限 | **任务强制「允许」档**：任务引擎全局权限写 allow，不弹确认框；创建向导中明示风险 |

## 4. 总体架构

```
┌─────────────────────────── XWork 主进程 ───────────────────────────┐
│                                                                    │
│  定时器(Scheduler) ── tick 命中 ──► 任务执行器(TaskRunner)          │
│        ▲                                    │                      │
│        │                                    ▼                      │
│  xwork-tasks.json                    任务引擎(TaskEngine)          │
│  (任务定义+历史)                      ─ 独立进程实例 ─             │
│                                          │ port: 4096+N           │
│                                          │ xdgHome: <home>/tasks/  │
│                                          │ cwd: 任务工作区         │
│                                          │ opencode.json: allow    │
│                                          ▼                        │
│  Bridge(复用) ── sendMessage / 事件流 ──► 完成/失败 ──► 通知       │
│                                                                    │
│  主引擎(现有) 完全不受影响                                           │
└────────────────────────────────────────────────────────────────────┘
```

要点：

- **任务引擎**与主引擎完全隔离：不同端口、不同数据目录、不同工作区，互不干扰
- **Bridge 复用**：任务引擎暴露同样的 HTTP/SSE 接口，可直接复用现有 `Bridge` 的 `sendMessage`、`listSessions` 等能力（端口可配）
- **多实例可行性已验证**：`engine.js` 的 spawn 逻辑中，`xdgEnv(xdgHome)` 通过环境变量隔离 config/state/data，第二个实例只需不同端口 + 不同 `xdgHome` + 不同 `cwd`

## 5. 数据模型

存储文件：`xwork-tasks.json`（位于引擎数据目录，与 `xwork-settings.json` 同级）

### 5.1 任务定义

```json
{
  "id": "task-xxx",                          // 唯一 ID
  "name": "每日晨会摘要",                     // 显示名
  "workspace": "d:/repo",                    // 任务运行的工作区
  "prompt": "读取 CHANGELOG.md 生成昨日变更摘要，输出到 daily.md",
  "model": { "providerID": "xgroup-abc", "modelID": "deepseek-chat" },
  "agent": "build",                          // build | plan
  "schedule": "0 9 * * *",                   // cron 表达式（唯一存储格式）
  "enabled": true,
  "timeoutMs": 1800000,                      // 单次执行超时，默认 30 分钟
  "createdAt": 1754802000000,
  "lastRunAt": null,
  "lastResult": {                            // 最近一次执行结果
    "status": "completed",                   // completed | failed | timeout | skipped
    "summary": "已生成 daily.md（3 个文件变更）",
    "durationMs": 42000,
    "endedAt": 1754802042000
  }
}
```

### 5.2 简化模板 → cron 翻译（UI 层完成，存储只有 cron 一种格式）

| 简化模板 | cron |
| --- | --- |
| 每天 HH:MM | `MM H * * *` |
| 每周 W 天 HH:MM（周日=0） | `MM H * * W` |
| 每 N 小时 | `0 */N * * *` |
| 应用启动时 | 特殊标记（`@startup`），不走 cron 匹配 |
| 高级：自定义 cron | 原样存储，需校验合法性 |

## 6. 调度器设计

- **实现位置**：主进程，`setInterval` 每分钟 tick 一次
- **匹配逻辑**：比对当前时间与每个启用任务的 `nextRunAt`，命中即触发执行，执行后重算下一次
- **cron 解析**：手写最小 5 字段解析器（分/时/日/月/周），约 40 行；若倾向成熟方案，可引入 `cron-parser` 依赖（取舍见 §12）
- **错过执行策略**（应用启动/休眠恢复时）：
  - 若任务时间点已过、且该任务今天尚未执行过 → **补跑一次**
  - 连续错过多次 → 跳过，等待下个周期

## 7. 任务引擎（独立进程）

### 7.1 启动参数

```js
// 与主引擎 Engine 实例同构，仅参数不同：
port:    effectiveConfig().enginePort + 1      // 4097
xdgHome: path.join(mainXdgHome, 'tasks')       // 独立数据目录
cwd:     任务.workspace                          // 目标工作区
```

### 7.2 配置写入

任务引擎启动前，向其 `config/opencode/opencode.json` 写入：

- `permission`：全部操作类型 = `allow`（含 question 的处理见 §9）
- `provider`：复用主设置的模型组配置（与 `applyToOpencode` 同构，`apiKey` 仍用 `{env:VAR}` 引用，启动时注入环境变量）

### 7.3 生命周期

| 时机 | 行为 |
| --- | --- |
| 首个任务执行前 | 惰性 spawn（冷启动数秒） |
| 任务执行期间 | 常驻 |
| 空闲 5 分钟无任务 | 停止进程，释放资源 |
| 应用退出 | 随应用退出 |

## 8. 执行流水线

```
tick 命中
  │
  ├─► 1. 确保任务引擎在线（spawn / 复用）
  ├─► 2. 定位任务会话（固定会话 ID，复用上下文）或新建
  ├─► 3. 注入 prompt 占位符（{{date}} 等）
  ├─► 4. bridge.sendMessage(会话, prompt, model, agent)
  ├─► 5. 监听该会话 session.idle / session.error
  ├─► 6. 判定结果：完成 / 失败 / 超时
  ├─► 7. 更新任务 lastRunAt / lastResult（持久化）
  └─► 8. 系统通知（完成 / 失败），点击通知可查看结果
```

### 8.1 任务会话策略（待定，见 §13）

- **固定会话复用**：每个任务绑定一个固定会话 ID，跨次执行累积上下文 —— 适合日报、迭代类任务
- **每次新建会话**：每次执行独立 —— 适合一次性巡检类任务

### 8.2 占位符模板

| 占位符 | 含义 | 示例 |
| --- | --- | --- |
| `{{date}}` | 今天 | `2026-08-10` |
| `{{date:-1d}}` | 昨天 | `2026-08-09` |
| `{{date:+7d}}` | 一周后 | `2026-08-17` |

支持简单相对偏移（`±Nd`），用于「生成昨日变更摘要」这类需求。

## 9. 安全与无人值守

- **权限强制 allow**：任务引擎的 `opencode.json` 全局权限写 allow，执行过程中不弹权限确认框
- **提问工具（question）处理**：allow 档不覆盖 `question` 类型。方案：任务会话收到 `question.asked` 时**自动选择第一项作为默认回答**，并在任务历史中标记「已默认应答」；该行为在创建向导中明示
- **创建向导风险提示**：创建任务时明确告知「任务将无人值守自动执行，权限为允许档，AI 可能修改工作区文件」

## 10. 失败处理与生命周期

| 场景 | 策略 |
| --- | --- |
| 执行超时（默认 30min） | abort + 标记 `timeout` + 通知 |
| 任务引擎启动失败 / 模型不可用 | 重试 2 次（退避），仍失败 → 标记 `failed` + 通知 |
| 应用启动时已过时间点 | 补跑一次（今天未执行过才补） |
| 电脑休眠期间 | 唤醒后按「错过执行策略」处理 |

## 11. UI 设计

### 11.1 入口

- 标题栏Skill Hub按钮左侧新增「定时任务」按钮
- 点击后切换到定时任务管理页 

### 11.2 任务列表（tab 主视图）

卡片式任务列表，每项展示：

- 任务名 + 启停开关
- 下次执行时间（cron 人话描述）
- 最近结果（状态徽标：完成/失败/超时/跳过 + 摘要 + 耗时）
- 运行中状态（执行中动画）
- 操作按钮：「立即执行」「编辑」「删除」

### 11.3 创建/编辑向导（弹窗）

1. **选工作区**：复用 `workspace:list` 下拉选择
2. **写 prompt**：多行文本，支持占位符提示
3. **选模型 / agent**：复用模型组下拉 + build/plan 切换
4. **选频率**：简化模板（每天/每周/每 N 小时/启动时）+ 高级 cron 输入
5. **风险确认**：无人值守 + 允许档权限提示

### 11.4 任务历史

点击任务卡片展开最近执行记录：时间、状态、耗时、结果摘要；失败可查看错误信息。

## 12. 取舍与风险

| 项 | 说明 |
| --- | --- |
| 双进程内存 | 任务引擎常驻期间多一个 opencode 进程；以「空闲 5 分钟回收」缓解 |
| 会话隔离 | 任务会话不出现在主 UI 会话列表（数据目录不同）；任务历史由应用自记 |
| question 默认应答 | 唯一有「AI 自主性」的点，创建时明示，历史中留痕 |
| cron 解析 | 手写最小解析器 vs 引入 `cron-parser`：后者成熟但增加依赖；倾向手写 |
| 任务引擎冷启动 | 首次执行需数秒拉起引擎，属可接受延迟 |

## 13. 待定问题（评审时确认）

- [ ] **任务会话策略**：固定复用 vs 每次新建（默认建议：固定复用，可配置）
- [ ] **超时/重试参数**：使用默认值（30min / 2 次）还是要 UI 可配
- [ ] **入口位置**：除设置页 tab 外，是否加侧栏快捷入口
- [ ] **历史保留**：每个任务保留最近 N 次记录（默认 20？），超出是否清理
- [ ] **cron 解析**：手写 vs 依赖（影响是否新增 npm 包）

---

## 附录：实现涉及文件（预估）

| 文件 | 变更 |
| --- | --- |
| `app/src/main/tasks.js`（新增） | 任务存储、调度器、执行器、任务引擎管理 |
| `app/src/main/index.js` | 注册任务 IPC、启动调度器、接入通知 |
| `app/src/main/bridge.js` | 端口可配置（复用现有方法） |
| `app/src/preload/index.js` | 暴露任务相关 API |
| `app/src/renderer/src/App.jsx` | 设置页「定时任务」tab + 创建向导 |
| `app/src/renderer/src/styles.css` | 任务列表/向导样式 |
| `ui-mockup/index.html` | 同步演示界面 |
